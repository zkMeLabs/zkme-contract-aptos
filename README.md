### zkMe Contract on Aptos

Build:

```shell
aptos move compile
```

Test:

```shell
aptos move test
```

Deploy:

```shell
aptos move publish --named-addresses zkme=zkme-account --url [network url] --private-key [private key]
```
